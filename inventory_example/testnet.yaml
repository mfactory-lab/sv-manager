---
all:
  children:
    testnet_validators:
      children:
        remote:
          hosts:
            server1:
              ansible_host: <ip address>
              #any var from group vars:
              validator_name: "<validator name>"
              validator_homepage: '<your homepage>'
              keybase_username: '<your keybase name>'
            server2:
              ansible_host: <ip address>
        local:
          hosts:
            localhost:
              #any var from group vars:
              validator_name: "Localhost"