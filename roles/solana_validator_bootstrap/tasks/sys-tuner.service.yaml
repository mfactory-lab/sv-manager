---
- name: Create solana-sys-tuner service
  template:
    src: solana-sys-tuner.service.j2
    dest: /etc/systemd/system/solana-sys-tuner.service
    mode: 0644
    owner: root
    group: root
  tags:
    validator.service.sys-tuner

- name: solana-sys-tuner service enable and start
  systemd:
    name: solana-sys-tuner
    enabled: yes
    state: restarted
    daemon_reload: yes
  tags:
    validator.service.sys-tuner

